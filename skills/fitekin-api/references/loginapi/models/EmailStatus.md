# EmailStatus

Enum (string). JSON bodies use the member name; search restrictions and numeric query parameters use the number.

- `NotExist` = 1
- `ExistsVerified` = 2
- `ExistsNotVerified` = 3
- `ExistsNoVerificationCheck` = 4

Used by:

- GET /api/Login/CheckEmailExists (response) — [Login](../endpoints/Login.md)
