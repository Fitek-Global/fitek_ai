# FitekIN Web API: PartnerAuthentication

Base path: `{BASE_URL}/webapi`. Header `Authorization-Token: <session token>` (see ../auth.md).

1 endpoint(s). Models are described under `../models/`; enum values under `../enums.md`.

## GET /api/PartnerAuthentication/ValidateSessionTokenTsekkEE

**Parameters**

_No parameters._

**Request body**

_None._

**Response (200)**

[SessionTsekkEEDTO](../models/SessionTsekkEEDTO.md)
