# TerminationReasons

Enum (string). JSON bodies use the member name; search restrictions and numeric query parameters use the number.

- `Logout` = 0
- `CompanySwitch` = 1
- `Kick` = 2
- `Relogin` = 3

Used by:

- [BackOfficeSessionDTO](BackOfficeSessionDTO.md).TerminationReason
