# ConfirmValidationIssueType

Enum (string). JSON bodies use the member name; search restrictions and numeric query parameters use the number.

- `TransactionRow` = 0
- `CustomField` = 1
- `TotalMismatch` = 2

Used by:

- [ConfirmValidationIssueDTO](ConfirmValidationIssueDTO.md).Type
