# InvoiceConfirmOutcome

Enum (string). JSON bodies use the member name; search restrictions and numeric query parameters use the number.

- `Approved` = 0
- `ValidationFailed` = 1
- `ApproveFailed` = 2

Used by:

- [InvoiceConfirmResultDTO](InvoiceConfirmResultDTO.md).Outcome
