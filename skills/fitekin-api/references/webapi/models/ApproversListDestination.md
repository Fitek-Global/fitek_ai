# ApproversListDestination

Enum (string). JSON bodies use the member name; search restrictions and numeric query parameters use the number.

- `Invoice` = 0
- `PurchaseOrder` = 1

Used by:

- [GroupMemberApproversParamDTO](GroupMemberApproversParamDTO.md).ApproversListDestination
